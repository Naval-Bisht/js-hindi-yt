"use strict";  // for newer strict
// alert(3+3)   // this will give the pop-up in the window in broweset but in the termial it will give error as now it not used any more;
let a=10;

console.log(typeof null);  // obect as output 
console.log(typeof undefined);   // undefined is the ans

